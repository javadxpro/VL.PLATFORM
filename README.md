# 🖥️ Local_Network — Volexturn Super-App v4.0 (Public)

یک سوپر‌اپلیکیشن **آفلاین و شبکه محلی (LAN)** با تجربه‌ی **تلگرام + اینستاگرام + یوتیوب** — چت شخصی و گروهی، شبکه اجتماعی کامل و ویدیو — بدون نیاز به اینترنت و با امنیت واقعی.

🇮🇷 پروژه ایرانی | 🔒 تمام حقوق محفوظ است — استفاده فقط با اجازهٔ کتبی | نسخه Public v4.0.0

---

## 🆕 تازه‌های نسخه 4.0 (موتور)

بدون تغییر در رابط کاربری، بک‌اند از یک فایل تک‌سرور به یک بستهٔ ماژولار تبدیل شد:

- 🧱 **ساختار ماژولار:** `backend/` با ۱۴ ماژول API، لایهٔ دسترسی، مهاجرت‌های نسخه‌دار (v1…v11) و `server.py` به‌عنوان یک shim باریک — مسیرهای قدیمی (`/login`، `/users`، `/posts` …) سرِ جایشان و با همان شکل پاسخ کار می‌کنند.
- 🔐 **حذف حساب پیش‌فرض `admin/admin123`:** ادمین اول فقط از localhost و با «کد راه‌اندازی» یک‌بارمصرف ساخته می‌شود، یا از CLI.
- 🔑 **نشست‌های واقعی:** توکن بearer با انقضا، چرخش (rotation) در هر ورود، سقف نشست برای هر کاربر، و ابطال فوری هنگام خروج/تغییر رمز/بن.
- 🧯 **اعتبارسنجی سخت‌گیرانه:** رمز عبور با PBKDF2-SHA256 (پیش‌فرض ۲۴۰٬۰۰۰ تکرار) + ارتقای خودکار هش‌های قدیمی در اولین ورود؛ Rate-limit با سطل‌های کرانه‌دار؛ CSRF با بررسی Origin برای متدهای خطرناک.
- 👁️ **حریم خصوصی در SQL:** فید، استوری و پست‌ها بر اساس سطح دسترسی (public/followers/friends/private) در خود کوئری فیلتر می‌شوند، نه بعد از گرفتن داده.
- 📦 **آپلود امن:** نام فایل تولیدی است، نوع با magic bytes تشخیص داده می‌شود (نه از روی پسوند)، `.svg` رد می‌شود، و فایل یتیم جارو می‌شود.
- 🖥️ **CLI:** `python -m backend run|migrate|create-admin|passwd|token|routes|doctor|janitor|sweep`.
- 🆔 **VL ID:** هر حساب یک شناسهٔ عمومیِ یکتا و غیرقابل‌تغییر (`VL-XXXX-XXXX`) دارد که در پروفایل‌ها و لیست کاربران دیده می‌شود؛ از شمارهٔ داخلی حساب ساخته نمی‌شود تا قابل اسکراب نباشد (migration `0011`، `docs/ROADMAP.md` §P1).
- 🧪 **تست:** ۲۰۴ تست pytest که قرارداد HTTP فرانت‌اند و قوانین دسترسی را می‌پوشانند (`python -m pytest tests/`).
- 🌐 **نسخهٔ نمایشی روی GitHub Pages:** همان رابط، بدون سرور و با دادهٔ محلیِ مرورگر — <https://javadxpro.github.io/VL.PLATFORM/> (`pages/vl-demo.js` + `tools/build_pages.py`، توضیح کامل در `docs/PAGES.md`).
- 🧳 **سالمت عملیاتی:** `/healthz`، هدرهای امنیتی، `Dockerfile` با کاربر بدون امتیاز و HEALTHCHECK، `render.yaml`، `.env.example` و GitHub Actions.

---

## 🆕 تازه‌های نسخه 3.0

- 👥 **گروه‌ها:** ساخت گروه، افزودن/حذف عضو، چت گروهی زنده، خروج/حذف گروه، بج خوانده‌نشده گروه
- ↪️ **هدایت پیام** به چند مخاطب و گروه هم‌زمان (با لیبل «هدایت‌شده»)
- 🖼️ **محدودیت هوشمند:** عکس ۵MB با **فشرده‌سازی خودکار** (Canvas)، فیلم ۱۰MB، فایل ۵۰MB
- 🔐 **پروتکل‌های امنیتی:**
  - رمزها با **PBKDF2-SHA256** (پیش‌فرض ۲۴۰,۰۰۰ تکرار + Salt) هش می‌شوند — هش‌های قدیمی در اولین ورود خودکار ارتقا می‌یابند
  - **توکن نشست** برای همه API ها (Bearer Token) + پاسخ بدون رمز عبور
  - API های ادمین فقط با نقش ادمین (403 برای بقیه)
  - **Rate-limit** روی ورود/ثبت‌نام (ضد Brute-force)
  - هدرهای امنیتی: CSP، nosniff، Referrer-Policy، Permissions-Policy
  - اعتبارسنجی ورودی + جلوگیری از جعل هویت فرستنده
- 🏭 **نسخه Publish:** ترمینال توسعه حذف، دیباگ خاموش، اجرای پایدار

---

## ✨ ویژگی‌ها

### 💬 چت در سبک تلگرام
- پیام متنی، عکس، ویدیو، فایل و **پیام صوتی** — Real-time با Socket.IO
- ↩️ **ریپلای** به پیام خاص (با پرش به پیام اصلی)
- ✏️ **ویرایش پیام** + لیبل «ویرایش شده»
- 🗑️ حذف پیام برای هر دو طرف
- ✓✓ **تیک دیده‌شده**
- ⌨️ نشانگر **«در حال نوشتن...»** زنده
- 🔴 **بج پیام خوانده‌نشده** روی هر مخاطب
- 📌 **سنجاق پیام** مهم در بالای گفتگو
- 🔍 جستجو در تاریخچه گفتگو

### 📸 شبکه اجتماعی در سبک اینستاگرام
- ❤️ لایک پست با **انیمیشن دابل‌تپ**
- 💬 کامنت زیر پست‌ها
- 👤 **فالو/فالوور** + شمارنده و پروفایل عمومی
- #️⃣ **هشتگ‌های کلیک‌خور** و فیلتر اکسپلور با هشتگ
- 👀 **آمار بیننده‌های استوری** (چه کسی استوری من را دید؟)
- 🔔 **فید اعلان‌ها**: لایک، کامنت و فالو جدید — زنده
- استوری ۲۴ ساعته با حذف خودکار

### ▶️ ویدیو در سبک یوتیوب
- 👁️ **شمارنده بازدید** ویدیوها
- 🔥 تب **ترند** (مرتب‌سازی بر اساس لایک/کامنت/بازدید)
- 🕐 **تاریخچه تماشا**
- ✅ فید «دنبال‌شده‌ها»

### بقیه امکانات
- 🎙️ **ویس‌چت زنده گروهی** با WebRTC (P2P)
- 🎮 **بازی LAN:** ثبت و اشتراک هاست‌های بازی با کپی یک‌کلیکی IP
- 🛡️ **پنل مدیریت:** آمار زنده (لایک/کامنت/فالو/پیام/...)، مدیریت کاربران و پست‌ها
- 🌗 تم تاریک/روشن • 🔌 **کاملاً آفلاین** (فونت و Socket.IO لوکال) • 💾 SQLite • نشست پایدار بین رفرش‌ها

---

## 🚀 نصب و اجرا

### پیش‌نیازها
- Python 3.8+

### ۱️⃣ کلون مخزن
```bash
git clone https://github.com/javadxpro/Local_Network.git
cd Local_Network
```

### ۲️⃣ نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

### ۳️⃣ بررسی پیش‌از‌اجرا (اختیاری ولی توصیه‌شده)
```bash
python3 -m backend doctor          # محیط، دیتابیس، آپلود، سیاست کشف شبکه، realtime
python3 -m backend doctor --json   # برای CI — در صورت خطا کد خروج ۱
```
این دستور فقط گزارش می‌دهد و چیزی را درست نمی‌کند؛ مهاجرت دیتابیس در اولین اجرای
سرور خودکار انجام می‌شود (یا دستی: `python3 -m backend migrate`).

### ۴️⃣ اجرای سرور
```bash
python3 server.py                  # حالت ساده
python3 -m backend run             # همان سرور، با بannر و کنترل‌های عملیاتی
PORT=5001 python3 -m backend run   # پورت دیگر
```
محیط‌ها از فایل `.env` خوانده می‌شوند؛ تمام متغیرهای پشتیبانی‌شده با مقدار
پیش‌فرض در `.env.example` فهرست شده‌اند.

### ۵️⃣ باز کردن در مرورگر
```
http://localhost:5000
```
از دستگاه‌های دیگر روی همان شبکه: `http://IP_سرور:5000`

---

## 🔑 اولین حساب مدیر

حساب پیش‌فرضی وجود ندارد — `admin/admin123` در نسخهٔ 4.0 حذف شده و اگر دیتابیس
قدیمی شما همچنان آن رمز را داشته باشد، `doctor` آن را به‌عنوان خطا گزارش می‌کند.

**ساخت ادمین با CLI (توصیه‌شده):**
```bash
python3 -m backend create-admin -u root -p 'یک-رمز-بلند-و-منحصربه‌فرد'
python3 -m backend passwd root            # رمز تازه می‌گذارد (بی‌ -p می‌پرسد) و نشست‌های او را باطل می‌کند
python3 -m backend token root             # فقط development؛ یک توکن خام چاپ می‌کند (‏--json مستند)
```

`create-admin` قابل دوباره‌اجراست: بدون `-p` روی حسابی که از قبل ادمین است فقط
می‌گوید «already» و با کد ۰ بیرون می‌آید (مناسب entrypoint)، با `-p` رمز را
بازنشانی می‌کند. `token` در `VOLEXTURN_ENV=production` با کد ۲ رد می‌شود.

**ساخت ادمین از مرورگر:** تا وقتی هیچ ادمینی نیست، صفحهٔ راه‌اندازی یک
«کد راه‌اندازی» یک‌بارمصرف می‌خواهد که در لاگ سرور چاپ می‌شود؛ این مسیر فقط از
`localhost`/`127.0.0.1` کار می‌کند و به‌محض وجود اولین ادمین بسته می‌شود.

---

## 📁 ساختار پروژه

```
Local_Network/
├── server.py              # shim: `server:app` برای gunicorn + اجرای مستقیم
├── backend/               # موتور برنامه
│   ├── app.py             # create_app، blueprint ها، مسیرهای قدیمی، هدرها
│   ├── api/               # ۱۴ ماژول: auth, users, posts, stories, messages,
│   │                      #   groups, gaming, servers, rooms, notifications,
│   │                      #   search, reports, admin, meta
│   ├── config.py          # تنها منبع واقعیت برای تنظیمات (از env/.env)
│   ├── db.py              # SQLite/Postgres، کوئری‌های پارامتری، SQL سازگار
│   ├── migrations.py      # v1…v10 (افزودنی، idempotent)
│   ├── auth.py security.py visibility.py feed.py uploads.py storage.py
│   ├── realtime.py notify.py presence.py discovery.py janitor.py
│   └── cli.py             # python -m backend …
├── index.html             # رابط کاربری کامل (HTML/CSS/JS - بدون وابستگی خارجی)
├── pages/
│   ├── vl-demo.js         # موتور نسخهٔ نمایشی: fetch را محلی سرو می‌کند (برای GitHub Pages)
│   └── demo-selftest.js   # ۵۴ بررسی روی همان موتور، با node و بدون فریمورک تست
├── tests/                 # ۱۵۹ تست pytest (قرارداد API + امنیت + CLI)
├── docs/API.md            # مرجع مسیرها — از خود برنامه تولید می‌شود
├── docs/LEGACY_MAP.md     # نگاشت مسیر قدیمی → endpoint جدید
├── docs/security.md       # چه اقدامی فعال است و چه چیزی عمداً انجام نشده
├── docs/TERMUX.md         # اجرا روی گوشی (Termux) — نصب، wake-lock، بوت خودکار
├── docs/ROADMAP.md        # قفل فازها: چه چیزی در نسخهٔ فعلی است و چه چیزی نه
├── docs/PAGES.md          # نسخهٔ نمایشی روی GitHub Pages: چه چیزی هست و چه چیزی عمداً نیست
├── docs/AUDIT.md          # گزارش ممیزی نسخهٔ قبلی و هر اصلاح انجام‌شده
├── tools/gen_api_docs.py  # تولیدکنندهٔ docs/API.md و docs/LEGACY_MAP.md
├── tools/build_pages.py   # بیلد نسخهٔ نمایشی Pages (فقط stdlib؛ بیلد commit نمی‌شود)
├── termux.sh              # همان مراحل Termux در یک اسکریپت (install / run / boot)
├── LICENSE                # تمام حقوق محفوظ است — هیچ مجوزی نمی‌دهد
├── THIRD_PARTY_NOTICES.md # مجوز قطعه‌های شخص ثالث که در مخزن آمده‌اند
├── requirements.txt  requirements-core.txt  .env.example
├── Dockerfile  Procfile  render.yaml  run.sh
├── static/
│   ├── js/socket.io.min.js   # کلاینت Socket.IO (لوکال)
│   └── fonts/…               # فونت وزیرمتن (لوکال)
├── uploads/               # فایل‌های آپلودی (profiles, chat, stories, posts)
└── database.db            # دیتابیس SQLite (خودکار ساخته می‌شود)
```

---

## 🛠️ فناوری‌ها

| بخش | فناوری |
|------|--------|
| بک‌اند | Python 3, Flask, Flask-SocketIO, SQLite |
| فرانت‌اند | HTML5, CSS3 (Glassmorphism), JavaScript |
| Real-time | Socket.IO |
| ویس‌چت | WebRTC (P2P) |
| فونت | Vazirmatn (لوکال) |

---

## 📋 راهنمای استفاده

### 💬 چت
1. بعد از ورود، از تب **چت‌ها** یک نفر را انتخاب کنید
2. پیام بفرستید یا با 📎 فایل و با 🎙️ ویس ضبط کنید

### 🎬 اکسپلور
- استوری بگذارید (۲۴ ساعت اعتبار دارد)
- پست متنی + عکس/ویدیو منتشر کنید

### 🎮 بازی LAN
1. در تب **بازی LAN** روی «هاست جدید» بزنید
2. نام بازی، IP و پورت را وارد کنید
3. بقیه با یک کلیک، آدرس را کپی می‌کنند

### 🎙️ ویس‌چت
- در تب **ویس‌چت** دکمه اتصال را بزنید و اجازه دسترسی به میکروفون بدهید

---

## 📱 اجرا در Termux (اندروید)

```bash
apt update && apt install python git -y
pip install -r requirements.txt
git clone https://github.com/javadxpro/Local_Network.git
cd Local_Network
python server.py
```

---

## 🔒 نکات امنیتی

- این پلتفرم برای **شبکه محلی** طراحی شده؛ آن را مستقیم روی اینترنت عمومی قرار ندهید.
- کنترل‌های مدیریتی **سمت سرور** اعمال می‌شوند (`auth="admin"`)، نه فقط پنهان‌شدن تب
  «مدیریت» در رابط کاربری؛ همین rule برای هر مسیر `/api/admin/*` برقرار است.
- نشست‌ها انقضا دارند، در هر ورود می‌چرخند، و با تغییر رمز/بن/خروج ابطال می‌شوند.
- `VOLEXTURN_TRUST_XFF` را فقط وقتی روشن کنید که واقعاً پشت یک proxy باشید؛ در غیر
  این صورت هر کاربر می‌تواند با هدر ساختگی از rate-limit فرار کند.
- کشف شبکه (probe) پیش‌فرض خاموش است؛ با روشن‌کردنش فقط IPهای داخل allowlist و
  بدون اسکن پروب می‌شوند، و آدرس‌های رزرو شده/متادیتا ابرها همیشه رد می‌شوند.
- آپلودها: نام فایل تولید می‌شود، نوع با magic bytes تشخیص داده می‌شود، و مسیر
  `/files/<category>/<name>` اجازهٔ خروج از پوشهٔ خودش را نمی‌دهد.

---

## 🐛 حل مشکلات

**پورت اشغال است:** با `PORT=5001 python3 -m backend run` (یا `VOLEXTURN_PORT`) عوضش کنید — در کد دست نبرید.

**ویس‌چت کار نمی‌کند:** میکروفون باید به مرورگر اجازه داده شود؛ در شبکه‌های ایزوله، WebRTC از کاندیدهای محلی (host candidates) استفاده می‌کند.

**ویدیوی حجم بالا آپلود نمی‌شود:** سقف‌ها تنظیم‌پذیرند — `VOLEXTURN_MAX_VIDEO_MB` (پیش‌فرض ۱۰)، `VOLEXTURN_MAX_FILE_MB` (۵۰)، `VOLEXTURN_MAX_IMAGE_MB` (۵). اگر پشت nginx هستید، `client_max_body_size` هم باید بالا برود.

---

## 📜 لایسنس

این پروژه **متن‌باز نیست**. `LICENSE` هیچ حقی نمی‌دهد — اجرا، میزبانی، کپی، تغییر،
توزیع، fork، و استفاده برای آموزش مدل هم نه؛ حتی استفادهٔ شخصی و غیرانتفاعی نیاز به
اجازهٔ کتبی دارد. کامپوننت‌های شخص ثالثی که در مخزن آمده‌اند (کلاینت Socket.IO با
مجوز MIT و فونت وزیرمتن با OFL 1.1) مجوز خودشان را نگه می‌دارند: جدولشان در
`THIRD_PARTY_NOTICES.md` است.

## 👨‍💻 نویسنده

ساخته شده با ❤️ توسط [javadxpro](https://github.com/javadxpro)

🇮🇷 **Iran** | 🌟 اگر این پروژه مفید بود، ستاره بدهید!
