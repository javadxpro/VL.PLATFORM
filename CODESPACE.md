# 🚀 اجرای Volexturn روی GitHub Codespaces

Codespaces یک کامپیوتر ابری رایگان داخل گیت‌هابه — بدون نیاز به سیستم روشن در لحظه اجرا و بدون باز کردن هیچ پورتی روی مودمت.

---

## ✅ مزایا
- HTTPS خودکار → **میکروفون کار می‌کند** 🎤
- لینک عمومی می‌توانی بسازی و به دوستان بدهی
- دیتابیس و آپلودها داخل Codespace می‌مانند (تا وقتی آن را حذف نکنی)
- رایگان: ماهی ۱۲۰ ساعت-هسته (با هسته ۲تایی ≈ **۶۰ ساعت در ماه**)

---

## 📋 مراحل اجرا (یک‌بار برای همیشه)

### ۱️⃣ کد را روی GitHub پوش کن
```bash
git push
```
(یا فایل‌ها را از طریق وب‌سایت GitHub در مخزن آپلود کن — مهم اینکه `devcontainer` و بقیه فایل‌ها باشند)

### ۲️⃣ ساخت Codespace
- در صفحه مخزن، دکمه سبز **Code** را بزن
- برو به تب **Codespaces** → **Create codespace on main**
- ۱ تا ۲ دقیقه صبر کن — وابستگی‌ها **خودکار نصب می‌شوند** (به لطف `devcontainer.json`)

### ۳️⃣ اجرای سرور
در ترمینال Codespace:
```bash
bash run.sh
```

### ۴️⃣ عمومی‌کردن لینک ⚠️ (مهم‌ترین قدم)
- در پایین VS Code، تب **Ports** را باز کن
- پورت **5000** را پیدا کن (اسمش: Volexturn Web App)
- روی آیکون قفل/گوشه پورت **راست‌کلیک** → **Port Visibility** → **Public**
- حالا لینک را **کپی** کن (Ctrl+Click روی لینک یا راست‌کلیک → Copy):
  ```
  https://<شناسه>-5000.app.github.dev
  ```

این لینک را به هرکسی بدهی، از هر جای دنیا پلتفرم را می‌بیند 🎉

---

## 🔄 نکات مهم

| نکته | توضیح |
|------|-------|
| ⏸️ خاموشی خودکار | بعد از **۳۰ دقیقه بی‌استفاده** Codespace می‌خوابد؛ از مخزن → Code → Codespaces دوباره بازش کن و `bash run.sh` بزن. (لینک ثابت می‌ماند ✅) |
| ⏱️ سهمیه رایگان | حدود ۶۰ ساعت در ماه — وقتی کارت تمام شد از صفحه Codespaces روی `…` → **Stop codespace** بزن تا سهمیه نسوزد |
| 🔒 امنیت | اولین کار: وارد شو → پروفایل → **تغییر رمز عبور** |
| 🗑️ حذف | اگر Codespace را حذف کنی، **همه دیتا (کاربران/پیام‌ها/ویدیوها) پاک می‌شود** — برای بکاپ، فایل `database.db` و پوشه `uploads` را کپی کن |
| 🎤 میکروفون | چون HTTPS است، روی گوشی و PC درخواست مجوز می‌آید و کار می‌کند |

---

## ❓ اگر لینک برای بقیه باز نشد
۹۹٪ یعنی پورت **Public نشده** — مرحله ۴ را دوباره چک کن (علامت قفل کنار پورت باید **گلوبال** باشد نه قفل).
