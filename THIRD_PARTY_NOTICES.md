# حقوق اشخاص ثالث / Third-party notices

مخزن تحت `LICENSE` «تمام حقوق محفوظ است» قرار دارد و هیچ حقی برای استفاده نمی‌دهد.
آن مجوز فقط ناظر بر **کد و متن‌های خودِ Volexturn** است. قطعه‌هایی که در زیر می‌آیند
مالکیت این پروژه ادعا نمی‌شود و هرکدام مجوز خودشان را دارند؛ این فایل برای همین
نوشته شده تا مرز مشخص باشد — و هم این نکته که **هر توزیع این پروژه باید این مجوزها
را هم رعایت کند**، چون مجوز ما چیزی است که مالک آن هستیم.

## ۱) فایل‌هایی که داخل مخزن آمد‌ه‌اند (redistributed)

| فایل | کامپوننت | پرنده | مجوز | نکته |
|---|---|---|---|---|
| `static/js/socket.io.min.js` | Socket.IO client v4.5.0 | © 2014-2022 Guillermo Rauch | MIT | سرریزش در همان فایل (`/*! … */`) است؛ ما فقط کپی محلی‌اش کرده‌ایم تا UI به CDN وصل نشود |
| `static/fonts/Vazirmatn-*.woff2` (۴ وزن) | فونت وزیرمتن | © Saeid Ebrahimi (rastikerdar) — Vazirmatn | SIL Open Font License 1.1 | OFL اجازهٔ استفاده و جاسازی فونت را می‌دهد؛ اجازهٔ فروش خود فونت را نه. نام فونت هم در اثر مشتق نباید تغییر کند |

هیچ فایل دیگری از بیرون در مخزن کپی نشده است: `index.html` هیچ `https://` ندارد
(چک‌شدن: `grep -oE "https?://" index.html`).

## ۲) وابستگی‌هایی که pip نصب می‌کند

اینها در مخزن نیستند؛ در runtime نصب می‌شوند و مجوز خودشان را نگه می‌دارند.

| بسته | مجوز |
|---|---|
| Flask | BSD-3-Clause |
| Werkzeug | BSD-3-Clause |
| Flask-SocketIO | MIT |
| python-socketio · python-engineio | MIT |
| simple-websocket | MIT |
| python-dotenv | BSD-3-Clause |
| gunicorn | MIT |
| gevent | MIT |
| psycopg / psycopg2 (اختیاری، فقط PostgreSQL) | LGPL-3.0 |

هیچ‌کدام در کد ما تغییر نمی‌کنند و فقط import می‌شوند، پس تعارضی با `LICENSE`
ایجاد نمی‌کند. اگر روزی forkی از یکی از اینها داخل این مخزن بیاید، مجوزش باید
همین‌جا فهرست و کنار فایلش گذاشته شود.

## ۳) دو پیامد عملی

1. **کسی نمی‌تواند با برداشتن `LICENSE` خودش را مالک بداند**؛ فایل بخشی از اثر است و
   حذفش مجوز را از بین نمی‌برد.
2. **این پروژه «متن‌باز» نیست**، اما `README` و `DEPLOY.md` هم نباید چنین القا کنند؛
   اگر جایی چنین نوشتی، همان‌جا را اصلاح کن. مجوز خود کامپوننت‌های بالا اجازهٔ
   استفاده از *خودِ آن کامپوننت‌ها* را می‌دهد — نه از این نرم‌افزار.

## ۴) اگر می‌خواهی روزی واقعاً آزادش کنی

تصمیم آگاهانه‌ای است، ولی قبلش این دو را انجام بده: (الف) هر قطعه‌ای که از جای
دیگر کپی شده را با مجوز سازگارش فهرست کن؛ (ب) از همهٔ مشارکت‌کنندگان — و از
مخاطبانِ «اجازهٔ کتبی» که شاید روی نسخه‌شان حساب باز کرده‌اند — اجازهٔ تغییر مجوز
بگیر، چون اعطای حق بعد از انتشار قابل‌پس‌گرفتن نیست. انتخاب‌های متعارف:
MIT (کوتاه‌ترین)، GPL-3.0-or-later (اگر می‌خواهی مشتق‌ها هم آزاد بمانند)،
AGPL-3.0-only (اگر می‌خواهی میزبان‌های SaaS هم ملزم به انتشار منبع شوند).
